/* active groups */
Group:Switch:OR(ON, OFF) Lights "[(%d)]"  <light>

/* Lights */
Switch light_id0 <light> (Lights)
Switch light_id1 <light> (Lights)

/* MQTT binding */
String mqtt_pub "[%s]" {mqtt=">[broker:toIoT:command:*:default]"}
String mqtt_sub "[%s]" {mqtt="<[broker:fromIoT:state:default]"}